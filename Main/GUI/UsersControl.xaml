<UserControl x:Class="Main.UsersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Main"
             mc:Ignorable="d" 
             d:DesignHeight="420" d:DesignWidth="660"
             Background="white">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- DataGrid -->
        <DataGrid x:Name="ListOfUsers_DataGrid" Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" HorizontalAlignment="Left" Margin="0,63,0,20" Width="640" Background="White" BorderThickness="1" BorderBrush="#ccc" RowHeight="30" MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Width="80" Header="Id" Binding="{Binding Id}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Width="180" Header="Name" Binding="{Binding Name}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Width="180" Header="Surname" Binding="{Binding Surname}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Width="180" Header="Email" Binding="{Binding Email}">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
            <DataGrid.Resources>
                <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource {x:Type ScrollBar}}">
                    <Setter Property="Background" Value="#eee"/>
                    <Setter Property="Foreground" Value="#ccc"/>
                    <Setter Property="Width" Value="10"/>
                </Style>
                <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource {x:Type ScrollBar}}" x:Key="CustomVerticalScrollBarStyle">
                    <Setter Property="Background" Value="#eee"/>
                    <Setter Property="Foreground" Value="#ccc"/>
                    <Setter Property="Width" Value="10"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter
                                              VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <Grid x:Name="UserInfo_Grid" Margin="290,0,20,0" Grid.RowSpan="2" Width="350" Visibility="Hidden">
            <!-- Imię -->
            <StackPanel Margin="28,2,12,370" Orientation="Horizontal" Width="310">
                <!-- Etykieta i pole tekstowe dla imienia -->
                <StackPanel Width="150">
                    <Label Content="Name:"/>
                    <TextBox x:Name="Name_TextBox" Text="Jan" IsEnabled="False" Height="20"/>
                </StackPanel>

                <!-- Etykieta i pole tekstowe dla nazwiska -->
                <StackPanel Margin="10,0,0,0" Width="150">
                    <Label Content="SubName:"/>
                    <TextBox x:Name="SubName_TextBox" Text="Kowalski" IsEnabled="False" Height="20"/>
                </StackPanel>
            </StackPanel>


            <!-- Nazwisko -->

            <!-- Adres zamieszkania -->
            <GroupBox Header="Address" Margin="24,220,12,89">
                <Grid Margin="0,5,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="80"/>
                        <ColumnDefinition Width="53*"/>
                        <ColumnDefinition Width="Auto" MinWidth="85.998"/>
                        <ColumnDefinition Width="15*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Po lewej stronie -->
                    <Label Content="City:" Grid.Row="0" Grid.Column="0" Margin="0,0,0,24" Grid.RowSpan="2" Height="27"/>
                    <TextBox x:Name="City_TextBox" Text="Warszawa" Grid.Row="0" IsEnabled="False" Margin="1,4,6,2" Grid.Column="1" Height="20"/>

                    <Label Content="Street:" Grid.Column="0" Margin="0,25,0,4" Grid.RowSpan="2" Height="27"/>
                    <TextBox x:Name="Street_TextBox" Text="Krakowska" Grid.Row="1" IsEnabled="False" Margin="1,4,6,3" Grid.Column="1" Height="20"/>

                    <Label Content="Apartament :" Grid.Column="2" Margin="2,25,2,4" Grid.RowSpan="2" Height="27"/>
                    <TextBox x:Name="ApartamentNumber_TextBox" Text="3A" Grid.Row="1" Grid.Column="3" IsEnabled="False" Margin="2,2,2,5" Height="20"/>

                    <!-- Po prawej stronie -->
                    <Label Content="Postal Code:" Grid.Row="2" Grid.Column="0" Margin="0,0,0,44" Height="27"/>
                    <TextBox x:Name="PostalCode_TextBox" Text="00-001" Grid.Row="2" IsEnabled="False" Margin="1,4,6,46" Grid.Column="1" Height="20"/>

                    <Label Content="House Nr. :" Grid.Row="0" Grid.Column="2" Margin="2,2,2,25" Grid.RowSpan="2" Height="27"/>
                    <TextBox x:Name="HouseNumber_TextBox" Text="12" Grid.Row="0" Grid.Column="3" IsEnabled="False" Margin="2,4,2,2" Height="20"/>
                </Grid>
            </GroupBox>


            <!-- Numer PESEL -->
            <StackPanel Margin="28,107,172,266" Height="47">
                <Label Content="PESEL:"/>
                <TextBox x:Name="PESEL_TextBox" Text="12345678901" IsEnabled="False" Height="20"/>
            </StackPanel>

            <!-- Data urodzenia -->
            <StackPanel Margin="28,55,172,318" Height="47" Width="150">
                <Label Content="Date of Birth:"/>
                <TextBox x:Name="DateOfBirth_TextBox" Text="01.01.1990" IsEnabled="False" Height="20"/>
            </StackPanel>

            <!-- Płeć -->
            <StackPanel Margin="188,55,12,318" Height="47" Width="150">
                <Label Content="Gender:"/>
                <ComboBox x:Name="Gender_ComboBox" IsEnabled="False" Height="20">
                    <ComboBoxItem Content="Female"/>
                    <ComboBoxItem Content="Male"/>
                </ComboBox>
            </StackPanel>

            <!-- Adres e-mail -->
            <StackPanel Margin="26,159,14,211">
                <Label Content="Email:"/>
                <TextBox x:Name="Email_TextBox" Text="jan.kowalski@example.com" IsEnabled="False" Height="20"/>
            </StackPanel>

            <!-- Numer telefonu -->
            <StackPanel Margin="188,107,12,266" Height="47">
                <Label Content="Phone Number:"/>
                <TextBox  x:Name="PhoneNumber_TextBox" Text="123456789" IsEnabled="False" Height="20"/>
            </StackPanel>
            <Label Content="Field for buttons (user modification)" HorizontalAlignment="Left" Height="70" Margin="26,336,0,0" VerticalAlignment="Top" Width="310" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Background="#4CCCCCCC"/>
        </Grid>

        <Label x:Name="SearchParametrs_Label" Content="Field to search for users in the list" HorizontalAlignment="Left" Height="63" Grid.RowSpan="2" VerticalAlignment="Top" Width="640" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Background="#4CCCCCCC"/>
    </Grid>
</UserControl>
